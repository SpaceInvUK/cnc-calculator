# cnc-calculator

**This branch (`main`) is intentionally empty except for a private placeholder.**

The public GitHub Pages site was taken down on 2026-09-20 by decision of the repository
owner. All content — the calculator HTML files, the VCarve gadgets, the prototypes and the
docs — lives on the **`app`** branch (`git checkout app`).

Do not restore files to `main`: GitHub Pages serves this branch, so anything committed here
becomes publicly readable on the internet.
